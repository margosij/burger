<h1>Burgers</h1>

<h2>Burgers that still need to be eaten!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <div class="false">{{this.burger_name}}</div>
      <button data-devourBurger = "{{this.id}}" class = "dev-burger">Devour that burger!</button>
    {{/unless}}
  {{/each}}
</ul>

<h2>Burger devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <div class="consumed">{{this.burger_name}}</div>
    {{/if}}
  {{/each}}
</ul>

<h2>Add Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger:</label>
    <input type="text" id="input" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>